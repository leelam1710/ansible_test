- name: setup roboshop Project
  hosts: all
  become: yes
  tasks:
    - name: import frontend
      ansible.builtin.import_role:
        name: frontend
      when: APP == "frontend"

    #- name: import catalogue
      #ansible.builtin.import_role:
        #name: catalogue
      #when: APP == "catalogue"